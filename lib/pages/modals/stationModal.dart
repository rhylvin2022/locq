import 'package:flutter/material.dart';

class StationModal extends StatelessWidget {
  const StationModal({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
